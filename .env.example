# Choose one or both providers. Keep this file private and never commit .env.
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional provider-specific defaults. Change these if your account uses a different model slug.
OPENAI_MODEL=gpt-5.6-luna
OPENROUTER_MODEL=~typesafe/jev-latest
FILLME_PORT=8788
